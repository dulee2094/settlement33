# 🚀 로컬 테스트 가이드

Render에 배포하지 않고 로컬에서 빠르게 테스트하는 방법들입니다.

## 📋 목차
1. [자동 테스트 (추천)](#자동-테스트)
2. [브라우저 수동 테스트](#브라우저-수동-테스트)
3. [API 직접 테스트](#api-직접-테스트)

---

## 🤖 자동 테스트 (추천)

### 방법 1: 빠른 테스트 (가장 간단!)

```bash
# 터미널 1: 서버 실행
npm start

# 터미널 2: 테스트 실행
npm run test:quick
```

**특징:**
- ✅ 색상으로 구분된 결과 출력
- ✅ 테스트 계정 정보 자동 생성
- ✅ 전체 플로우 자동 검증
- ✅ 약 2-3초 소요

### 방법 2: 상세 검증 테스트

```bash
npm run test:blind
```

**특징:**
- ✅ 더 상세한 검증 로직
- ✅ 각 단계별 상태 확인
- ✅ 동기화 검증 포함

---

## 🌐 브라우저 수동 테스트

### 준비
```bash
npm start
```

브라우저에서 `http://localhost:3000` 접속

### 시나리오: 블라인드 제안 테스트

#### 1단계: 가해자 계정 (Chrome 일반 모드)

1. **회원가입**: `http://localhost:3000/signup.html`
   - 이메일: `offender@test.com`
   - 비밀번호: `1234`
   - 이름: `가해자`
   - 전화번호: `010-1111-1111`

2. **로그인** → 대시보드

3. **사건 생성**
   - 사건번호: `TEST-001`
   - 역할: 가해자
   - 요약: `테스트 사건`

4. **블라인드 제안**
   - 금액: `5,000,000`원
   - 제출 후 "상대방 제안 대기 중" 확인

#### 2단계: 피해자 계정 (Chrome 시크릿 모드)

1. **회원가입**: `http://localhost:3000/signup.html`
   - 이메일: `victim@test.com`
   - 비밀번호: `1234`
   - 이름: `피해자`
   - 전화번호: `010-2222-2222`

2. **로그인** → 대시보드

3. **사건 연결**
   - 사건번호: `TEST-001`
   - 역할: 피해자

4. **블라인드 제안**
   - 금액: `8,000,000`원
   - 제출 후 **분석 결과 확인**
   - 차이: 3,000,000원 표시 확인

#### 3단계: 결과 확인

- 가해자 창으로 돌아가서 새로고침
- 양측 모두 분석 결과 표시 확인
- 중간점 합의 여부 확인

---

## 🔧 API 직접 테스트 (고급)

### PowerShell에서 API 테스트

```powershell
# 서버 실행 확인
Invoke-WebRequest http://localhost:3000

# 회원가입
$body = @{
    email = "test@test.com"
    password = "1234"
    name = "테스터"
    phoneNumber = "010-0000-0000"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:3000/api/signup" `
    -Method POST `
    -ContentType "application/json" `
    -Body $body
```

### 브라우저 콘솔에서 테스트

```javascript
// F12 → Console 탭

// 제안 상태 확인
fetch('/api/case/proposal?caseId=1&userId=1')
    .then(r => r.json())
    .then(console.log);

// 제안 제출
fetch('/api/case/proposal', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        userId: 1,
        caseId: 1,
        amount: 5000000,
        duration: 1
    })
})
    .then(r => r.json())
    .then(console.log);
```

---

## 💡 유용한 팁

### 데이터베이스 초기화
테스트 데이터가 꼬였을 때:

```bash
# Windows PowerShell
Remove-Item database.sqlite
npm start
```

### 여러 사용자 동시 테스트
- **Chrome 일반 모드**: 사용자 1
- **Chrome 시크릿 모드**: 사용자 2  
- **Edge/Firefox**: 사용자 3

### 개발자 도구 활용
1. **F12** → **Network** 탭
   - API 호출 확인
   - 요청/응답 데이터 확인

2. **Console** 탭
   - 에러 메시지 확인
   - localStorage 확인:
     ```javascript
     console.log(localStorage.getItem('user_id'));
     console.log(localStorage.getItem('current_case_id'));
     ```

3. **Application** 탭
   - localStorage 직접 수정
   - 쿠키 확인

---

## 📊 테스트 비교

| 방법 | 속도 | 자동화 | 상세도 | 난이도 |
|------|------|--------|--------|--------|
| `npm run test:quick` | ⚡⚡⚡ | ✅ | ⭐⭐ | 쉬움 |
| `npm run test:blind` | ⚡⚡ | ✅ | ⭐⭐⭐ | 쉬움 |
| 브라우저 수동 | ⚡ | ❌ | ⭐⭐⭐⭐ | 보통 |
| API 직접 | ⚡⚡ | ❌ | ⭐⭐⭐⭐⭐ | 어려움 |
| Render 배포 | 🐌 | ❌ | ⭐⭐⭐⭐ | 어려움 |

---

## 🎯 추천 워크플로우

### 일상적인 개발
```bash
# 1. 서버 실행
npm start

# 2. 코드 수정

# 3. 빠른 테스트
npm run test:quick

# 4. 문제 없으면 브라우저에서 확인
```

### 배포 전 최종 확인
```bash
# 1. 전체 테스트
npm run test:blind

# 2. 브라우저 수동 테스트

# 3. 문제 없으면 Render 배포
```

---

## ❓ 문제 해결

### "Server is NOT reachable" 에러
```bash
# 서버가 실행 중인지 확인
npm start
```

### 테스트 실패 시
1. 서버 로그 확인
2. `database.sqlite` 삭제 후 재시작
3. 브라우저 캐시 삭제

### 포트 충돌 시
```bash
# server.js에서 포트 변경 (기본: 3000)
# 또는 실행 중인 프로세스 종료
```

---

## 📚 추가 자료

- `tests/quick_test.js` - 빠른 테스트 스크립트
- `tests/verify_blind_function_v2.js` - 상세 검증 스크립트
- `tests/browser_test_guide.md` - 브라우저 테스트 가이드
